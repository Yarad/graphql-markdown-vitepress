# About this demo

This page is a hand-written markdown file outside the GraphQL docs. The
plugin's stylesheet should **not** restyle anything here — h3s, the page
title, and brand colours all stay at VitePress defaults.

## A regular section

Some intro text to verify body copy is unchanged.

### A leaky-suspect h3

If the scope is wrong this h3 will turn uppercase, shrink to `0.82rem`, get
a top border, and pick up a `padding-top` of `1.5rem`. With the scope class
applied correctly it stays at the VitePress default (`20px`, no border, no
transform).

Some text after the h3.

### Another h3

More body text.
